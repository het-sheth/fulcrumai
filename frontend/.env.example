# Backend API URL
VITE_API_URL=http://localhost:8000

# EmailJS Configuration (for sending email notifications)
# Sign up at https://www.emailjs.com/ (free tier: 200 emails/month)
# 1. Create an email service (Gmail, Outlook, etc.)
# 2. Create a template with variables: to_email, event_title, event_date, event_location, event_description, calendar_link, action_text
# 3. Copy your IDs below
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key
